<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.peterr.qrgen">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/qricon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/qricon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MainActivity1"
            android:label="@string/title_activity_main1"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".GenActivity"
            android:label="@string/title_activity_gen"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".GennActivity"
            android:label="@string/title_activity_genn"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".AuthAct"
            android:label="Scan Authentication Code">
                   </activity>
    </application>

</manifest>